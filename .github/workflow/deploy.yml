name: CI/CD Pipeline

on:
  push:
    branches: [ main ] # Jalan otomatis saat kamu push ke branch main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout kode dari GitHub
        uses: actions/checkout@v4

      - name: Login ke Docker Hub (Tempat simpan hasil bungkus)
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      # Robot membungkus Backend
      - name: Build & Push Backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: username-kamu/backend:latest

      # Robot membungkus Frontend
      - name: Build & Push Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: username-kamu/frontend:latest